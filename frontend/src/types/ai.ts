export enum AiOption {
  OpenAi = "OPENAI",
  Gemini = "GEMINI",
  DeepSeek = "DEEPSEEK",
  // Claude = "CLAUDE",
}

export enum KeyStorePref {
  UNSET = "UNSET",
  LOCAL = "LOCAL",
  CLOUD = "CLOUD",
}
